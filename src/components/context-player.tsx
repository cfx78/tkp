'use client';
import { LockKeyhole, LoaderCircle, Pause, Play } from 'lucide-react';
import { usePlayer } from './player-provider';
import type { PlayerBeat } from '@/src/types/player';
import type { BeatFile } from '@/src/types/beat-file';
type ContextVersion = BeatFile['versions'][number]; type Parent = { _id: string; title: string; slug?: string; coverArtUrl?: string; lane?: PlayerBeat['lane'] };
export function ContextPlayer({ parent, versions }: { parent: Parent; versions: ContextVersion[] }) { const player = usePlayer(); const queue: PlayerBeat[] = versions.filter((item) => item.audioAvailable && !item.nsfw).map((item) => ({ _id: `version:${parent._id}:${item._key}`, sourceType: 'version', parentBeatId: parent._id, versionKey: item._key, title: item.title, versionType: item.versionType, parentBeatTitle: parent.title, parentBeatSlug: parent.slug, slug: parent.slug, coverArtUrl: parent.coverArtUrl, lane: parent.lane })); return <div className="border-t border-[var(--line-subtle)]">{versions.map((version, index) => { const trackId = `version:${parent._id}:${version._key}`; const active = player.beat?._id === trackId; const eligible = version.audioAvailable && !version.nsfw; const queueIndex = queue.findIndex((item) => item._id === trackId); const playVersion = () => active ? void player.togglePlayback() : void player.playQueue(queue, { type: 'context', title: `Context: ${parent.title}` }, queueIndex, false); return <article key={version._key} className={`grid min-w-0 grid-cols-[2rem_minmax(0,1fr)_auto] gap-3 border-b border-[var(--line-subtle)] py-5 ${active ? 'bg-white/[0.025] shadow-[inset_2px_0_0_var(--accent)]' : ''}`}><span className="type-numeric pt-1">{String(index + 1).padStart(2, '0')}</span><div className="min-w-0"><div className="flex flex-wrap items-baseline gap-x-3 gap-y-1"><h3 className="break-words font-semibold text-[var(--text-primary)]">{version.title}</h3><span className="type-protocol-label text-[9px] text-[var(--text-muted)]">{version.versionType || 'Context'}</span>{active ? <span className="type-protocol-label text-[9px]">Current</span> : null}</div>{version.createdAt ? <p className="type-numeric mt-2">{formatDate(version.createdAt)}</p> : null}{version.note ? <details className="mt-3"><summary className="focusable-surface min-h-11 w-fit cursor-pointer py-3 text-sm text-[var(--text-secondary)]">Context notes</summary><p className="type-small max-w-xl pb-2">{version.note}</p></details> : null}{version.nsfw ? <p className="type-small mt-3 text-[var(--warning)]"><LockKeyhole className="mr-1 inline h-4 w-4" />{version.nsfwReason || 'NSFW Context audio is locked.'}</p> : !version.audioAvailable ? <p className="type-metadata mt-3">Audio unavailable</p> : null}</div><div>{eligible ? <button type="button" onClick={playVersion} disabled={active && player.isLoading} className="icon-control focusable-surface" aria-label={`${active && player.isPlaying ? 'Pause' : active ? 'Resume' : 'Play'} ${version.title}`}>{active && player.isLoading ? <LoaderCircle className="h-4 w-4 animate-spin" /> : active && player.isPlaying ? <Pause className="h-4 w-4" fill="currentColor" /> : <Play className="h-4 w-4" fill="currentColor" />}</button> : version.nsfw ? <span aria-label="Context audio locked" className="grid h-11 w-11 place-items-center text-[var(--warning)]"><LockKeyhole className="h-4 w-4" /></span> : <span className="block h-11 w-11" />}</div></article>; })}</div>; }
function formatDate(value: string) { const date = new Date(value); return Number.isNaN(date.getTime()) ? value : new Intl.DateTimeFormat('en-US', { dateStyle: 'medium' }).format(date); }
